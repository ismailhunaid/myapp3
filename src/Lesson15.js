import React from 'react'

const Alert = ({alert}) => {
    return(
        alert !== null && (
            <div>
                please write some thing to search !!
            </div>
        )
     )
}

export default Alert;
